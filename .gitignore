# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Secrets / credentials — never commit these
.env
*.env
secrets.*
credentials.*
*WHISKER_PASSWORD*

# AWS deploy policy — contains your account ID (keep local, don't publish)
code/aws_dashboard_policy.json

# Scratch / old material — contains account IDs, pet UUIDs, presigned URLs, serials
_to_delete/
*litter_robot_code_output*.txt

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
.ipynb_checkpoints/

# Transient diagnostic / report output
schedule_diag.txt
report.txt
